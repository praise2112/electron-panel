{
  "name": "electron-panel-window",
  "description": "Enables creating a browser window in Electron that behaves like a Panel.",
  "version": "0.0.1",
  "main": "index.js",
  "types": "electron.d.ts",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp rebuild --release",
    "test": "mocha"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "chai-as-promised": "^7.1.1",
    "electron": "^12.0.2",
    "mocha": "^8.3.2",
    "node-gyp": "^7.1.2",
    "spectron": "^14.0.0"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  }
}
